[Service]
ExecStartPre=/bin/bash -c 'i=1; while [ $i -le 60 ]; do busctl call org.freedesktop.DBus /org/freedesktop/DBus org.freedesktop.DBus NameHasOwner s xyz.openbmc_project.Logging 2>/dev/null | grep -q "true" && break; sleep 1; i=$((i+1)); done'
